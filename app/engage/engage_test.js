'use strict';

describe('myApp.engage module', function() {

  beforeEach(module('myApp.engage'));

  describe('engage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var engageCtrl = $controller('engageCtrl');
      expect(engageCtrl).toBeDefined();
    }));

  });
});